<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application Light theme. -->
    <style name="Base.Theme.PetKuy" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="InterTextView" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/inter_regular</item>
    </style>

    <style name="Theme.PetKuy" parent="Base.Theme.PetKuy" />

    <style name="Theme.PetKuy.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PetKuy.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PetKuy.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>